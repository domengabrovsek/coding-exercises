/**
 * Function which accepts a list of numbers and returns the sum of numbers in both arrays.
 * @param {Array.<Number>} arr1
 * @param {Array.<Number>} arr2
 * @returns {Number} Sum of all elements
 */
const sumArrays = (arr1, arr2) => {
  // TODO: Implement this function
};

module.exports = {
  sumArrays
};
