/**
 * Function which accepts two arrays of numbers and returns the sum of numbers in both arrays.
 * Example: [1,2,3], [4,5,6] -> 6 + 15 -> 21
 * @param {Array.<Number>} arr1
 * @param {Array.<Number>} arr2
 * @returns {Number} Sum of all elements
 */
const sumArrays = (arr1, arr2) => {
  // TODO: Implement this function
};

/**
 * Function which accepts two arrays of numbers and returns the product of sum from even numbers in first array and odd numbers in second
 * Example: [1,2,3], [4,5,6] -> 2 * 5 -> 10
 * @param {Array.<Number>} arr1
 * @param {Array.<Number>} arr2
 * @returns {Number} Product of sums
 */
const sumArraysAdv = (arr1, arr2) => {
  // TODO: Implement this function
};

/**
 * Function which accepts two of numbers and returns the a new array with summed elements in same position
 * Example: [1,2,3], [4,5,6] -> [5,7,9]
 * @param {Array.<Number>} arr1
 * @param {Array.<Number>} arr2
 * @returns {Array.<Number>} Array with sums
 */
const sumArraysSamePosition = (arr1, arr2) => {
  // TODO: Implement this function
};

/**
 * Function which accepts a list of numbers and returns a string with all numbers included.
 * Example: [1,2,3] -> "1 2 3"
 * @param {Array.<Number>} arr
 * @returns {String} String with all numbers from array
 */
const arrToString = (arr) => {
  // TODO: Implement this function
};

/**
 * Function which accepts a list of numbers and returns a string with all even numbers included.
 * Example: [1,2,3,4,5] -> "2 4"
 * @param {Array.<Number>} arr
 * @returns {String} String with all even numbers from array
 */
const arrToStringEven = (arr) => {
  // TODO: Implement this function
};

/**
 * Function which accepts a list of numbers and returns reversed array of same numbers
 * Example: [1,2,3,4,5] -> [5,4,3,2,1]
 * @param {Array.<Number>} arr
 * @returns {Array.<Number>} Reversed array
 */
const reverseArr = (arr) => {
  // TODO: Implement this function
};

/**
 * Function which accepts a number and returns factorial of that number
 * Example: 5 -> 5 * 4 * 3 * 2 * 1 -> 120
 * @param {Number} number
 * @returns {Number} Factorial of given number
 */
const factorial = (n) => {
  // TODO: Implement this function
};

/**
 * Function which accepts a string and returns true if string is a palindrome
 * Example1: racecar -> true
 * Example2: racecars -> false
 * @param {String} word
 * @returns {Boolean} true if palindrome, false if not
 */
const isPalindrome = (n) => {
  // TODO: Implement this function
};

module.exports = {
  sumArrays,
  sumArraysAdv,
  arrToString,
  arrToStringEven,
  reverseArr,
  sumArraysSamePosition,
  factorial,
  isPalindrome
};
